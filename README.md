# cpp-ds-practice
c++数据结构练习
文件结构
cpp-ds-practice/
——linked_list/    //单链表的实现
——stack/    //栈的实现
——queue/  //循环队列的实现
链表：使用单个节点实现
核心：链表的指针指向下一个节点的位置，删除节点和反转节点时要注意指针的指向防止地址丢失
栈：使用可以自动扩容的数组实现
核心：使用栈顶指针实现先进后出
队列：使用数组和头尾下标实现
核心：花费一个位置也判断队列是否溢出，判断当前元素的数量==（ tail-head+size）mod(size),插入后tail的值=（tail+1）mod（size）
